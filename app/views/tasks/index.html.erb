<h1>Tasks</h1>

<% @tasks.each do |task| %>
  <div><input type="checkbox" <%= task.completed ? "checked": ""%>><%= link_to task.title, task_path(task.id) %></div>
<% end %>

<br>
<%= link_to 'Add task', tasks_new_path %>

<%# <%= link_to tasks_new_path do  %> %>
  <%# <%= task.completed? <i class="fa-regular fa-square-check"></i> : <i class="fa-solid fa-square-check"></i> %> %>
<%# <% end %> %>
